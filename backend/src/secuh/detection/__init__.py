"""Adaptadores de visión: pre-filtro de movimiento (OpenCV) y detector YOLO.

`motion.py` (MOG2, con máscara de zona) y `yolo.py` (con calentamiento del
modelo) implementan las interfaces ``MotionDetector`` y ``PersonDetector`` de
``secuh.core.ports``.
"""
