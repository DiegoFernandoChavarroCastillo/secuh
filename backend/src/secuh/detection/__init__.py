"""Adaptadores de visión: pre-filtro de movimiento (OpenCV) y detector YOLO.

Se implementan en la Fase 1 (`motion.py`, `yolo.py`) sobre las interfaces
``MotionDetector`` y ``PersonDetector`` de ``secuh.core.ports``.
"""
